version: '3.8'

services:
  crop-prediction-app:
    build: .
    container_name: crop-ai-app
    ports:
      - "8501:8501" # Streamlit UI
      - "8000:8000" # FastAPI Backend
    volumes:
      - ./models:/app/models
    restart: unless-stopped
    environment:
      - WORKERS=4
